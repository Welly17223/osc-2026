[package]
name = "bootloader"
version = "0.1.0"
edition = "2024"

[dependencies]
oslib = { version="0.1.0", path = "../oslib" }

[build-dependencies]
cc = "1.0"

[features]
default = ["qemu"]
qemu = []
orangePI = []

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
